# Declare arguments for snasync, each source could be either the mountpoint containing .snapshots child mountpoint, or .snapshots mountpoint itself, and it shall have its own prefix and targets
# An example config that syncs to both local warm backup and multiple remote cold backup is as follows:
# SNASYNC_ARGS='--source / --prefix pc_root --target /srv/backup/snapshots --target snasync@nas.home.lan:/srv/backup/snapshots --target snasync@nas.home2.lan:/srv/backup/snapshots --source /home --prefix pc_home --target /srv/backup/snapshots --target snasync@nas.home.lan:/srv/backup/snapshots --target snasync@nas.home2.lan:/srv/backup/snapshots'
